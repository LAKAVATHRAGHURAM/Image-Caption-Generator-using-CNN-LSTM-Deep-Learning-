# Image-Caption-Generator-using-CNN-LSTM-Deep-Learning-
A deep learning based image captioning system built using Convolutional Neural Networks (CNN) for image feature extraction and Long Short-Term Memory (LSTM) networks for language generation. The notebook demonstrates data processing, feature extraction, model training, and caption generation.
